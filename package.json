{
  "name": "lustre-hire-site",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:critical": "playwright test --grep @critical"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0"
  }
}
